# Brand Club - Shared/Global Configuration
# Used by all stacks (Traefik, databases, apps)

# ===== DOCKER REGISTRY =====
DOCKER_REGISTRY=ghcr.io
DOCKER_ORG=brandclub

# ===== TRAEFIK (Shared Reverse Proxy) =====
TRAEFIK_VERSION=v3.6
TRAEFIK_DOMAIN=traefik.brandclub.site

# Generate with: docker run --rm httpd:2.4-alpine htpasswd -nB admin password | sed -e s/\\$/\\$\\$/g
# TRAEFIK_AUTH=$(openssl passwd -apr1 --password admin)

LETSENCRYPT_EMAIL=admin@brandclub.site

# ===== COMMON SETTINGS =====
CLIENT_MAX_BODY_SIZE=50m
